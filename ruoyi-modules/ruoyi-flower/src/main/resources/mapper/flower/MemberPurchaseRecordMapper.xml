<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.dromara.flower.mapper.MemberPurchaseRecordMapper">

    <update id="updateOtherMemberInfoByUserID">
        UPDATE member_purchase_record
        SET
            `status` = 0,
            update_by = #{userId},
            update_time = NOW()
        WHERE
            create_by =  #{userId}
          AND `status` = 1
          AND id != #{id};
    </update>
</mapper>
