# AGENTS.md

## 项目目标

将当前仓库清洗为一个**标准、可维护、可交接、可部署**的工程项目。

本次工作的核心不是重写业务，而是进行**工程标准化治理**，包括但不限于：

- 目录结构梳理
- 启动方式统一
- 配置文件分层整理
- 冗余文件和历史遗留清理
- 文档补齐
- 构建与部署说明完善
- 日志、异常、命名、脚本等工程规范提升

---

## 项目背景理解

当前仓库为一个多模块 Java 项目，已识别出的关键结构包括：

- 根构建文件：`pom.xml`
- 主应用模块：`ruoyi-admin`
- 扩展模块：`ruoyi-extend`
- 独立服务：
    - `ruoyi-monitor-admin`
    - `ruoyi-snail-job-server`
- 脚本目录：`script/`
- Docker 编排目录：`script/docker/`
- 多环境配置文件：
    - `application.yml`
    - `application-dev.yml`
    - `application-prod.yml`

你在处理此项目时，必须优先围绕“**工程标准化清洗**”这一目标工作，而不是随意做业务重构。

---

## 总体工作原则

1. **先分析，后修改**
    - 先理解项目结构、模块关系、启动方式、配置分层，再进行改动。
    - 不允许一上来就大规模改文件。

2. **优先最小改动**
    - 以最小改动实现最大规范化收益。
    - 非必要不调整业务逻辑、不改接口语义、不改数据库结构。

3. **禁止无依据猜测**
    - 不确定的地方必须明确标注“待确认”。
    - 不允许凭空假设模块职责、环境用途、部署方式。

4. **分批执行**
    - 所有清洗和改造必须拆成多个批次。
    - 每一批都要说明目标、涉及文件、风险、验证方式。

5. **先低风险，后中风险**
    - 优先做：
        - 文档补齐
        - 目录说明
        - 冗余文件识别
        - 启动说明梳理
        - 配置说明补齐
        - .gitignore 优化
    - 后做：
        - 配置归并
        - 启动脚本统一
        - 模块结构调整
        - 依赖治理

6. **每轮改动后都要验证**
    - 改动必须伴随验证建议。
    - 如可执行，应优先执行编译、测试、启动或静态检查。
    - 若当前环境不具备运行条件，也要明确给出验证命令。

7. **输出必须具体**
    - 所有问题和建议尽量具体到目录、文件、类、配置项。
    - 避免空泛表述，例如“代码不够规范”“结构可以优化”。

---

## 本项目中的优先关注点

你在审计和清洗时，请重点检查以下方面：

### 1. 目录与模块职责
重点确认：

- 根目录是否存在历史遗留文件、无用途文件、重复说明文件
- `ruoyi-admin` 是否为主启动入口
- `ruoyi-monitor-admin` 与 `ruoyi-snail-job-server` 是否为独立部署服务
- `ruoyi-extend` 下模块职责是否清晰
- 是否存在目录命名不统一、职责不明确、位置不合理的问题

### 2. 配置分层
重点检查：

- `application.yml`
- `application-dev.yml`
- `application-prod.yml`

检查内容包括：

- 公共配置与环境差异配置是否混杂
- 端口、数据源、Redis、日志、注册中心、监控等配置是否重复
- 是否存在“应该放在 dev/prod 却写在公共配置”的问题
- 是否存在失效、遗留、重复、未引用配置项

### 3. 启动与部署方式
重点检查：

- Windows 启动脚本
- jar 启动方式
- jps/taskkill 管理逻辑
- Docker 编排文件
- 多服务启动顺序
- 端口占用说明
- 环境变量依赖说明

目标是让项目具备：

- 清晰的本地启动说明
- 清晰的生产部署说明
- 清晰的多服务关系说明

### 4. 文档缺失
重点补齐：

- 根 README
- 模块说明
- 本地开发启动说明
- 多环境配置说明
- Docker 使用说明
- 常见问题说明
- 独立服务用途说明

### 5. 工程规范
重点检查：

- 命名风格是否统一
- 是否有明显无效依赖
- 是否有重复脚本
- 是否有无用配置文件
- 是否有未使用资源文件
- 是否有显然废弃的代码或目录
- 日志输出是否混乱
- 异常处理是否存在明显不规范点

---

## 任务执行流程

你必须严格按以下流程工作：

### 阶段 A：审计
先只做分析，不做修改。

输出内容必须包括：

1. 项目技术栈
2. 模块结构
3. 启动入口
4. 配置文件分层情况
5. Docker / 脚本情况
6. 文档完整度
7. 冗余和风险点
8. 按高/中/低优先级给出整改建议

### 阶段 B：改造计划
在审计完成后，输出分批改造计划。

建议拆成以下批次：

- **第1批：无风险清理与文档补齐**
- **第2批：配置与脚本标准化**
- **第3批：启动与构建方式整理**
- **第4批：代码层工程规范优化**

每一批都要写清：

- 目标
- 涉及文件
- 风险等级
- 是否影响运行
- 验证方式

### 阶段 C：逐批执行
执行时遵循以下要求：

- 每次只执行一个批次
- 先列出拟修改文件
- 再说明修改原因
- 再进行修改
- 修改后给出变更摘要
- 最后给出验证建议或验证结果

### 阶段 D：收尾交付
最后必须输出：

1. 已完成清洗项清单
2. 未完成但建议后续处理的问题
3. 当前项目的标准启动方式
4. 当前项目的标准目录说明
5. 当前项目的交接说明

---

## 修改约束

以下内容默认**不要主动改动**，除非用户明确要求或证据充分：

- 核心业务流程
- 接口协议
- 数据库表结构
- 业务字段命名
- 外部依赖服务接入逻辑
- 权限逻辑
- 业务模块拆分方式
- 已上线服务的核心行为语义

如必须调整，必须先说明：

- 为什么要改
- 改动影响范围
- 风险点
- 如何回退

---

## 输出格式要求

### 审计阶段输出格式
请尽量使用以下结构：

- 一、项目概况
- 二、模块与目录结构分析
- 三、启动与部署分析
- 四、配置文件分析
- 五、文档与脚本分析
- 六、问题清单
- 七、整改优先级建议

### 执行阶段输出格式
请尽量使用以下结构：

- 本轮目标
- 拟修改文件
- 修改内容说明
- 风险说明
- 修改结果
- 验证方式 / 验证结果
- 下一步建议

---

## 风险控制要求

你必须始终控制以下风险：

1. **避免一次性大改**
2. **避免跨模块误改**
3. **避免把业务改动伪装成“标准化清洗”**
4. **避免删除看似无用但实际被间接依赖的文件**
5. **避免在未确认运行方式前重写脚本**
6. **避免在未确认环境差异前合并配置**

对于“可能可删”的文件，优先做法不是立刻删除，而是：

- 先标记
- 先列出依据
- 先建议确认
- 再处理

---

## 你应优先产出的成果

优先顺序如下：

### 优先级 1
- 标准化审计报告
- 分批改造计划
- README 初版
- 项目启动说明
- 模块说明文档

### 优先级 2
- 配置分层梳理
- .gitignore 优化
- 脚本用途说明
- Docker 文件说明
- 冗余文件候选清单

### 优先级 3
- 启动脚本规范化
- 依赖治理建议
- 日志与异常规范建议
- 测试/校验命令建议

---

## README 期望包含内容

如果需要补 README，请至少覆盖：

1. 项目简介
2. 技术栈
3. 模块结构说明
4. 环境要求
5. 构建命令
6. 本地启动方式
7. 多服务启动说明
8. 配置文件说明
9. Docker 使用方式
10. 常见问题
11. 目录结构示意
12. 维护建议

---

## 脚本整理要求

对 `script/` 目录进行处理时，优先完成：

- 每个脚本用途说明
- 启动脚本与停止脚本区分
- Windows 与 Linux 脚本区分
- Docker 脚本说明
- 明显失效脚本识别
- 端口、jar、进程管理方式说明

不要在没有充分依据的情况下重写全部脚本。

---

## Docker 文件整理要求

对 `script/docker/` 下的文件进行处理时：

- 识别每个 compose 文件用途
- 判断是否为不同环境、不同部署角色、不同阶段遗留
- 补说明，不先合并
- 如建议合并，必须先给出依据与风险

---

## 与用户沟通时的默认策略

当用户要求“标准化”“清洗”“整理成规范项目”时，默认理解为：

- 先审计
- 先出计划
- 再小步执行
- 每步可验证
- 不做高风险业务重构

除非用户明确要求，否则不要自行扩大任务边界。

---

## 最终目标定义

当以下条件基本满足时，可认为本轮“标准化清洗”达成阶段性目标：

- 项目能被新接手的人快速理解
- 启动方式明确
- 模块职责清晰
- 配置分层可解释
- 脚本和 Docker 文件有说明
- README 足以支撑本地开发和交接
- 冗余、遗留、风险点被系统梳理
- 变更过程可回溯、可验证

---

## 默认第一步指令

当开始工作时，请默认先执行这一目标：

**先不要修改任何文件，先对整个仓库做一次标准化审计，并输出具体到文件路径的审计报告与分批整改建议。**